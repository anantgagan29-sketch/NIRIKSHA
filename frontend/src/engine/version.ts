/** Bumped when the pipeline changes shape. Stamped onto every compliance run. */
export const APP_VERSION = "0.1.0";

/**
 * Version of the compliance rule pack. Recorded with every assessment so a
 * result can be reproduced later against the exact rules that produced it.
 */
export const RULE_PACK_VERSION = "2011.1.0";

/** Version of the rule evaluation engine itself, independent of the rules. */
export const ENGINE_VERSION = "0.1.0";
