fastapi
uvicorn[standard]
python-multipart
pillow
# Headless build: the server has no display, and plain opencv-python pulls in
# GUI libraries (libGL and friends) that are absent from a slim container, so
# it imports fine locally and fails on the host. No GUI function is used here.
opencv-python-headless
python-dotenv
numpy

# Gemini vision: product parsing and readability analysis both import
# `from google import genai`. This was missing from the original list, so a
# clean install could not start the app.
google-genai

# Supabase issues ES256 access tokens and publishes the public keys at its
# JWKS endpoint. The API verifies them itself: the browser sends a token, not
# a user id, so a caller cannot name someone else and read their history.
pyjwt

# PostgreSQL, where a deployment keeps its scans. The host gives a free
# instance a disk that is wiped on every deploy, which took the scan history
# with it; the store moved somewhere that outlives the container. Unset
# DATABASE_URL and the application falls back to the SQLite file, which is
# what a local checkout runs on.
psycopg[binary]
